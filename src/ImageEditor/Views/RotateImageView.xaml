<?xml version="1.0" encoding="UTF-8"?>
<StackLayout xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ImageEditor.Views.RotateImageView"
             Orientation="Horizontal"
             HorizontalOptions="CenterAndExpand"
             BindingContext="{Binding RotateImageNinetyDegree}">
    <Button Style="{StaticResource OptionsButtonStyle}"
            Text="CCW"
            Command="{Binding RotateImageCommand}"></Button>
    <Slider Minimum="0"
            Maximum="360"
            Value="0"
            BackgroundColor="Transparent"
            WidthRequest="200"
            
            MaximumTrackColor="#FF0000"
            MinimumTrackColor="#FF0000" />
    <Button Style="{StaticResource OptionsButtonStyle}"
            Text="CW"
            Command="{Binding RotateImageCommand}"></Button>
</StackLayout>